import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://gesty.dev',
  // commented out because using a custom domain instead of 'name.github.io'
  //base: '/astroBlog',
});
